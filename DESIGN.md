# 《长征·抉择》DESIGN.md

> 比赛赛道：AI游戏开发 | 指定模型：智谱 GLM-5.1 | 形态：Web 小游戏  
> 详细策划以 `长征-抉择-设计方案.docx`（v0.2）为准，本文件为开发速览。

---

## 1. 一句话定位

《中国式家长》式图文经营：有限行动点 + 五维资源 + 人物关系网 + 大模型对话/裁决/对决 + 史实分栏的长征科普游戏。完整局 20–25 分钟，快速演示 12 分钟。
交互骨架两条：**舞台屏**（人物立绘 + 对白 + 抉择）与**玩法板**（小游戏专属：题名 + 数值签 + 玩法区）——人物在舞台交代任务，玩法在板上做，结算再回舞台。

## 2. 比赛准入映射

| 准入条件 | 实现 |
|---|---|
| 完整游戏循环 | 开场 → 五幕路线 → 营地日行动 → 节点/小游戏 → 对决 → 多结局 |
| 核心 AI 决策调用指定大模型 | GLM-5.1：npc_chat / branch_judge / share_judge / night_* / quiz_* / minigame_review / ending_review |
| 每次 AI 决策有日志 | JSONL + 游戏内「行军记录」；source=GLM\|ERROR（无 MOCK） |
| 算法对抗 | 知识对决 human_vs_ai / ai_vs_ai，独立 session，日志成对 |

本地只做：小游戏手感判定、点选节奏推进、数值执行。智能判断一律走模型。

## 3. 核心循环（对标中国式家长）

```
路线图推进一幕
  → 过场（场景图 + 史实时间地点）
  → 营地日（1–3 日 × 2 行动点）
       交谈 / 小游戏 / 休息 / 学习 / 分享
  → 强制节点事件 + 小游戏
  → 知识对决
  → 夜间抉择（模型生成选项）
  → 下一幕 / 终局结算
```

## 4. 资源与关系

| 资源 | 范围 | 说明 |
|---|---|---|
| 体力 | 0–100 | 归零失败 |
| 粮食 | 0–20 | 为 0 则体力日降 |
| 士气 | 0–100 | 影响抉择与选项 |
| 信念 | 0–100 | 影响动摇结局 |
| 民心 | 0–100 | 解锁老乡支线 |

同伴（好感 0–100）：老班长 / 指导员 / 红小鬼 / 卫生员 / 老乡。delta 由模型返回。

## 5. 幕次结构

| 幕 | 节点 | 营地日 | 强制 |
|---|---|---|---|
| 开场 | 瑞金 | 1×2 | 人设、出发 |
| 一 | 于都—湘江 | 2×2 | 封锁、湘江、对决1 |
| 转折 | 遵义 | 1×2 | 对话/抉择、对决2 |
| 二 | 金沙江—泸定—雪山—草地 | 3×2 | 小游戏三连、对决3 |
| 收束 | 会宁 | 1×2 | 对决4、结局 |

## 6. 小游戏（每线只 1 手感核心；都在**玩法板**上玩）

1. **金色的鱼钩**：弯针成钩 → 咬钩起竿（漂相三档：晃/沉/黑漂）→ 分汤（AI 仲裁）
2. **泥地五子棋**：9×9 简易对手
3. **飞夺泸定桥**：横移闪避限时（45 秒）
4. **分糖**：点糖 → 点人给出，一次提交交模型判定（share_judge）
5. **夜岗**：5 个信号判断处置，口令联动夜校（学到口令会在第 2 个信号用上）
6. **夜校识字**：三关拼口令，写回 `tonightPassword`
7. **分汤 / 煮粥**：一口锅怎么分（AI 仲裁）
8. **陡坡拉人**：三次机会拽住下滑的战友

本地只判手感与节奏；**每次结算都走 `/api/decide`**（`minigame_review` / `share_judge`），写进 JSONL 日志。

## 7. 大模型调用点（16 类，服务端契约表见 `server/schema.js`）

`scene_gen` · `choice_hint` · `npc_chat` · `share_judge` · `minigame_review` · `branch_judge`  
`quiz_generate` · `quiz_answer_ai` · `quiz_judge` · `night_options` · `night_resolve`  
`act_review` · `ending_review` · `failure_review` · `study_report` · `sim_turn`（沙盘）

失败重试 2 次 → 记 `source=ERROR` 并在界面提示原因、给「重试」键（不编造兜底文案）。无 Key 直接报错。整局真调约 76 次（`npm run test:e2e` 实测并断言）；每次调用的响应都过同一张契约表，缺字段即重试。

## 8. 结局

同行 / 守望 / 未竟 / 星火（隐藏）。由 ending_review 综合资源轨迹、关系、抉择史映射；前端只表现。

## 9. 美术

纸黄 `#E8DCC8` · 墨 `#2C2416` · 红 `#8B2E2E` · 金 `#C4A35A` · 青灰 `#5C6B73`（唯一来源 `public/css/tokens.css`）  
水彩淡彩 + 做旧纸；实际落盘：场景图 21 张 + 立绘 14 张 + 环境床 8 条 + 事件图，见 `changzheng/docs/ASSETS.md`。
中文排版用五族自托管字体（正文宋 / 对白楷 / 标题毛笔 / 档案明朝 / 数字 Garamond），随包分发、跨机一致。

## 10. 技术

**前端无构建**：原生 ES Module + DOM 叙事 + Canvas 小游戏（`public/`，`npm start` 直接托管）· Node Express 代理 GLM / 日志 / 静态 · `.env` 与 `runtime-config.json` 双层配置（密钥永不进前端）。

前端内部按**「内核 + 事件总线 + IP 模块」**组织（SoC 思路）：模块之间不直接调用，只通过事件与只读快照协作；
模块集合不写死——新增模块/交互玩法只需加一个描述符并在清单里登记一行，内核与其它模块都不用改。
规矩由 `npm run qa:bus` 强制；架构与「怎么加」见 `changzheng/docs/BUS.md`。

## 11. 里程碑

| 阶段 | 交付 | 状态 |
|---|---|---|
| M0 | 策划 + 概念图 | ✅ |
| M1 | 路线图 + 营地日 + npc_chat + 日志 | ✅ |
| M2 | 资源/关系/branch_judge/分享 | ✅ |
| M3 | 小游戏接入 | ✅（8 个玩法，见 §6） |
| M4 | 对决 + 双 AI + 多结局 | ✅ |
| M5 | 史实档案、快速模式、验收脚本 | ✅（录屏待路演时做） |

M5 之后进入**视觉收口**：纸墨设计系统（模板 + 区块 + 动效 + 守卫）逐批打磨 25 个页面，
进度与口径在 `changzheng/docs/DESIGN-SYSTEM.md` §五（批 1–4 已完成）。

演示样品（现归档于 `_archive/sample/`，另有中间版本 `_archive/demo-v1/`）的篝火中枢、小游戏骨架、行军记录可在 M1–M3 复用升级。

## 12. 界面草图

```
舞台屏（对白 / 抉择 / 裁决）              玩法板（小游戏专属）
┌────────────────────────────┐         ┌────────────────────────────┐
│ 于都河 · 暮色      [角标]   │         │ 于都河 · 第 1 日            │
│ ◯ 立绘  老班长 · 炊事班长    │         │ 金色的鱼钩                  │
├────────────────────────────┤         │ [鱼篓 0][咬钩 2][竿 1/3]    │
│ 正文（墨显）/ 抉择选项       │   ⇒    ├────────────────────────────┤
│ ── 对白 ──                  │         │ 漂相三档 ……   [canvas]      │
│ [继续]                      │         │ 漂缓缓下沉——真口！起竿！     │
└────────────────────────────┘         │ [抛竿] [起竿 (空格)]        │
  顶栏：体力/粮食/士气/信念/民心 常驻     └────────────────────────────┘
  节奏：人物在舞台交代任务 → 玩法板上做 → 回舞台结算（叙事 + 继续）
```
