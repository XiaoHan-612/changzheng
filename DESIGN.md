# 《长征·抉择》DESIGN.md

> 比赛赛道：AI游戏开发 | 指定模型：智谱 GLM-5.1 | 形态：Web 小游戏  
> 详细策划以 `长征-抉择-设计方案.docx`（v0.2）为准，本文件为开发速览。

---

## 1. 一句话定位

《中国式家长》式图文经营：有限行动点 + 五维资源 + 人物关系网 + 大模型对话/裁决/对决 + 史实分栏的长征科普游戏。完整局 20–25 分钟，快速演示 12 分钟。

## 2. 比赛准入映射

| 准入条件 | 实现 |
|---|---|
| 完整游戏循环 | 开场 → 五幕路线 → 营地日行动 → 节点/小游戏 → 对决 → 多结局 |
| 核心 AI 决策调用指定大模型 | GLM-5.1：npc_chat / branch_judge / share_judge / night_* / quiz_* / minigame_review / ending_review |
| 每次 AI 决策有日志 | JSONL + 游戏内「行军记录」；source=GLM\|ERROR（无 MOCK） |
| 算法对抗 | 知识对决 human_vs_ai / ai_vs_ai，独立 session，日志成对 |

本地只做：小游戏手感判定、点选节奏推进、数值执行。智能判断一律走模型。

## 3. 核心循环（对标中国式家长）

```
路线图推进一幕
  → 过场（场景图 + 史实时间地点）
  → 营地日（1–3 日 × 2 行动点）
       交谈 / 小游戏 / 休息 / 学习 / 分享
  → 强制节点事件 + 小游戏
  → 知识对决
  → 夜间抉择（模型生成选项）
  → 下一幕 / 终局结算
```

## 4. 资源与关系

| 资源 | 范围 | 说明 |
|---|---|---|
| 体力 | 0–100 | 归零失败 |
| 粮食 | 0–20 | 为 0 则体力日降 |
| 士气 | 0–100 | 影响抉择与选项 |
| 信念 | 0–100 | 影响动摇结局 |
| 民心 | 0–100 | 解锁老乡支线 |

同伴（好感 0–100）：老班长 / 指导员 / 红小鬼 / 卫生员 / 老乡。delta 由模型返回。

## 5. 幕次结构

| 幕 | 节点 | 营地日 | 强制 |
|---|---|---|---|
| 开场 | 瑞金 | 1×2 | 人设、出发 |
| 一 | 于都—湘江 | 2×2 | 封锁、湘江、对决1 |
| 转折 | 遵义 | 1×2 | 对话/抉择、对决2 |
| 二 | 金沙江—泸定—雪山—草地 | 3×2 | 小游戏三连、对决3 |
| 收束 | 会宁 | 1×2 | 对决4、结局 |

## 6. 小游戏（每线只 1 手感核心）

1. **金色的鱼钩**：弯针 → 咬钩起竿（漂相三档）→ 分汤（AI 仲裁）
2. **过草地**：三路线 + 探路/分粮/拉战友
3. **飞夺泸定桥**：横移闪避限时
4. **分糖**：拖糖分配，逐颗判定
5. **夜岗**：5 信号辨敌友，口令联动夜校
6. **夜校识字**：三关拼口令，写回 tonightPassword

## 7. 大模型调用点

`npc_chat` · `branch_judge` · `share_judge` · `night_options` · `night_resolve`  
`quiz_generate` · `quiz_answer_ai` · `quiz_judge` · `minigame_review` · `ending_review`

失败重试 2 次 → 记 `source=ERROR` 并在界面提示原因、给「重试」键（不编造兜底文案）。无 Key 直接报错。整局真调约 50+ 次。

## 8. 结局

同行 / 守望 / 未竟 / 星火（隐藏）。由 ending_review 综合资源轨迹、关系、抉择史映射；前端只表现。

## 9. 美术

纸黄 `#E8DCC8` · 墨 `#2C2416` · 红 `#8B2E2E` · 金 `#C4A35A` · 青灰 `#5C6B73`  
水彩淡彩 + 做旧纸；场景 8–10 + 胸像 5 + 地图 1。

## 10. 技术

Vite+TS 前端（DOM 叙事 + Canvas 小游戏）· Node Express 代理 GLM/日志/静态 · `.env` 密钥。

## 11. 里程碑

| 阶段 | 交付 |
|---|---|
| M0 | 策划 + 概念图 |
| M1 | 路线图 + 营地日 + npc_chat + 日志 |
| M2 | 资源/关系/branch_judge/分享 |
| M3 | 小游戏接入 |
| M4 | 对决 + 双 AI + 多结局 |
| M5 | 史实档案、快速模式、录屏 |

演示样品（现归档于 `_archive/sample/`，另有中间版本 `_archive/demo-v1/`）的篝火中枢、小游戏骨架、行军记录可在 M1–M3 复用升级。

## 12. 界面草图

```
┌──────────────────────────────────────────┐
│ 体力 粮食 士气 信念 民心          [行军记录] │
├──────────────────────────────────────────┤
│              场景图 / 小游戏               │
├──────────────────────────────────────────┤
│ 叙事……  [话题A][B][C]   好感变化          │
│ [自由输入……] [发送]                        │
│ [交谈][小游戏][休息][学习][分享]   行动点 2 │
└──────────────────────────────────────────┘
```
