# 依赖
node_modules/

# 密钥与本机设置：绝不入库
.env
runtime-config.json

# 运行产物（可重新生成）——不带中间斜杠，按文件名匹配任意层级
session-full.jsonl
server-out.txt
server-err.txt
**/tests/e2e/artifacts/

# 编辑器 / 系统
.DS_Store
Thumbs.db
*.log

# 归档区（_archive/）只跟踪代码与文档：
# 里面的场景原画、截图、配音都是被 changzheng/ 取代的旧素材，保留在磁盘但不必入库，
# 避免仓库被 ~65MB 位图撑大。确实需要版本管理就删掉下面四行。
_archive/**/*.png
_archive/**/*.jpg
_archive/**/*.jpeg
_archive/**/*.wav

# ZCode 客户端的过程文件（计划稿等）：是工具产物，不是源码
.zcode/

# 音频素材同理（mp3 是 2026-09 起为整段朗诵放开的一种容器）
_archive/**/*.mp3
_archive/**/*.ogg
