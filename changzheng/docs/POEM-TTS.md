# 终局升华 · 诗与落款（音频对照表）

> 由 `node scripts/poem-manifest.mjs` 生成。诗与落款的唯一真源是 `data/poem.json`（屏幕字幕、逐字、配音都读它）。

## 七律·长征 · 毛泽东（一九三五年十月）

出处：《毛泽东诗词集》

- 句数：**8**（每句 7 字，两句一联）
- 逐句配音音色：**narr**（文件名 = sha1(voiceId + "|" + 文本) 前 16 位 + `_narr.wav`）
- 已就位：**0 / 8**
- 整段录音：**无**（放 `public/audio/poem/`，再把 poem.json 的 `audio.full` 填上）
- 时间轴已标：**0 / 8** 句（整段录音路线必须有；逐句路线可留空）

## 逐句配音（推荐：与现有 TTS 产线同一条路）

产出的 wav 放进 `public/audio/cache/`，**文件名必须与下表一致**，`/api/tts` 会自动命中；
没命中就静默降级——逐字改按 `poem.json` 的 `pace` 走，流程不阻塞。

| id | 位置 | 文本 | 文件名 | 时间轴 | 状态 |
|---|---|---|---|---|---|
| poem_1 | 第 1 联 | 红军不怕远征难， | `c0637a656843bf7d_narr.wav` | — | ⬜ 待生成 |
| poem_2 | 第 1 联 | 万水千山只等闲。 | `6a8e286ecd278787_narr.wav` | — | ⬜ 待生成 |
| poem_3 | 第 2 联 | 五岭逶迤腾细浪， | `3e4a76014c02e7d4_narr.wav` | — | ⬜ 待生成 |
| poem_4 | 第 2 联 | 乌蒙磅礴走泥丸。 | `882e947cc90ebaf4_narr.wav` | — | ⬜ 待生成 |
| poem_5 | 第 3 联 | 金沙水拍云崖暖， | `99bfa66e9208790a_narr.wav` | — | ⬜ 待生成 |
| poem_6 | 第 3 联 | 大渡桥横铁索寒。 | `03ca9e084df5ca4f_narr.wav` | — | ⬜ 待生成 |
| poem_7 | 第 4 联 | 更喜岷山千里雪， | `56ad448bdb196b93_narr.wav` | — | ⬜ 待生成 |
| poem_8 | 第 4 联 | 三军过后尽开颜。 | `9e9c1fd15592c2b1_narr.wav` | — | ⬜ 待生成 |

规格：单声道 wav，16k/22.05kHz，静音裁掉首尾；一句一个文件（不要连读，句间停顿交给屏幕）。
语气：**克制、不喊口号**——与史实回响同一档（见策划案 §2.6 的混音口径）。

## 整段录音（可选：一段真正的朗诵）

1. 把录音放进 `public/audio/poem/`（该目录**不入库**：`.gitignore` 里挡着，别人录的音频不进公开历史）；
2. 在 `data/poem.json` 的 `audio.full` 填文件名，并按录音给每句标 `startMs` / `endMs`；
3. 逐字就会跟着录音的已播毫秒走（`voice:progress` 那一路），逐句配音那一路自动让位。

走这条路时 `npm run qa:audio` 会核对"放进来的文件有没有被 `audio.full` 指到"，漏填会红。

落款：**两万五千里** · 一九三四年十月 · 于都 — 一九三五年十月 · 会宁
