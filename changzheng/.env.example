# 天津移动指定接口（赛道二）。必须配置 Key：本项目没有 MOCK 兜底，未配置时调用会报错并写入 source=ERROR

GLM_API_KEY=
# 路径必须带 /mgate/v1：少了这一段网关不报错、只静默回 204 空响应（看起来像"模型抽风"，实际是地址写错）
GLM_API_URL=http://111.32.22.35:32592/mgate/v1/chat/completions
# 赛制指定 glm-5.1。该网关 /mgate/v1/models 只挂了这一个模型，填别的会失败
GLM_MODEL=glm-5.1
# 思考档位：运行态已在 server/ai.js 用 chat_template_kwargs 关闭思考（本网关 glm-5.1 始终思考，
# 不关的话 reasoning 会吃光 max_tokens、小预算调用 content 全空）。此值仅在换回智谱端点时有意义。
# 可选 low | high | max；max 本网关不认（回 400/204），ai.js 会过滤掉。
GLM_REASONING_EFFORT=low
PORT=3001
